spring:
  application:
      name: config-server
  cloud:
      config:
        server:
          git:
            uri: git@github.com:rekkeb/demo-microservices.git
            searchPaths: config-resources
          defaultLabel: master

server:
  port: 8585
