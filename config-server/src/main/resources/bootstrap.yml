spring:
  application:
      name: config-server
  cloud:
      config:
        server:
          git:
            uri: git@bitbucket.org:rekkeb/demo-microservices.git
            searchPaths: config-resources
          defaultLabel: master

server:
  port: 8585
